<def f='linux-5.3.1/sound/core/pcm_compat.c' l='393' ll='437' type='int snd_pcm_ioctl_xfern_compat(struct snd_pcm_substream * substream, int dir, struct snd_xfern32 * data32)'/>
<use f='linux-5.3.1/sound/core/pcm_compat.c' l='696' u='c' c='snd_pcm_ioctl_compat'/>
<use f='linux-5.3.1/sound/core/pcm_compat.c' l='698' u='c' c='snd_pcm_ioctl_compat'/>
<doc f='linux-5.3.1/sound/core/pcm_compat.c' l='387'>/*
 * xfern ioctl nees to copy (up to) 128 pointers on stack.
 * although we may pass the copied pointers through f_op-&gt;ioctl, but the ioctl
 * handler there expands again the same 128 pointers on stack, so it is better
 * to handle the function (calling pcm_readv/writev) directly in this handler.
 */</doc>
