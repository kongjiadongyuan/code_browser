<dec f='linux-5.3.1/include/crypto/pkcs7.h' l='31' type='int pkcs7_validate_trust(struct pkcs7_message * pkcs7, struct key * trust_keyring)'/>
<use f='linux-5.3.1/certs/system_keyring.c' l='251' u='c' c='verify_pkcs7_signature'/>
<doc f='linux-5.3.1/include/crypto/pkcs7.h' l='28'>/*
 * pkcs7_trust.c
 */</doc>
<def f='linux-5.3.1/crypto/asymmetric_keys/pkcs7_trust.c' l='158' ll='187' type='int pkcs7_validate_trust(struct pkcs7_message * pkcs7, struct key * trust_keyring)'/>
<dec f='linux-5.3.1/crypto/asymmetric_keys/pkcs7_trust.c' l='188' type='int pkcs7_validate_trust(struct pkcs7_message * , struct key * )'/>
<use f='linux-5.3.1/crypto/asymmetric_keys/pkcs7_trust.c' l='188' c='pkcs7_validate_trust'/>
<use f='linux-5.3.1/crypto/asymmetric_keys/pkcs7_trust.c' l='188' u='a'/>
<doc f='linux-5.3.1/crypto/asymmetric_keys/pkcs7_trust.c' l='134'>/**
 * pkcs7_validate_trust - Validate PKCS#7 trust chain
 * @pkcs7: The PKCS#7 certificate to validate
 * @trust_keyring: Signing certificates to use as starting points
 *
 * Validate that the certificate chain inside the PKCS#7 message intersects
 * keys we already know and trust.
 *
 * Returns, in order of descending priority:
 *
 *  (*) -EKEYREJECTED if a signature failed to match for which we have a valid
 *	key, or:
 *
 *  (*) 0 if at least one signature chain intersects with the keys in the trust
 *	keyring, or:
 *
 *  (*) -ENOPKG if a suitable crypto module couldn&apos;t be found for a check on a
 *	chain.
 *
 *  (*) -ENOKEY if we couldn&apos;t find a match for any of the signature chains in
 *	the message.
 *
 * May also return -ENOMEM.
 */</doc>
