<dec f='linux-5.3.1/fs/notify/fsnotify.h' l='64' type='void __fsnotify_update_child_dentry_flags(struct inode * inode)'/>
<doc f='linux-5.3.1/fs/notify/fsnotify.h' l='60'>/*
 * update the dentry-&gt;d_flags of all of inode&apos;s children to indicate if inode cares
 * about events that happen to its children.
 */</doc>
<def f='linux-5.3.1/fs/notify/fsnotify.c' l='104' ll='139' type='void __fsnotify_update_child_dentry_flags(struct inode * inode)'/>
<use f='linux-5.3.1/fs/notify/fsnotify.c' l='158' u='c' c='__fsnotify_parent'/>
<doc f='linux-5.3.1/fs/notify/fsnotify.c' l='97'>/*
 * Given an inode, first check if we care what happens to our children.  Inotify
 * and dnotify both tell their parents about events.  If we care about any event
 * on a child we run all of our children and set a dentry flag saying that the
 * parent cares.  Thus when an event happens on a child it can quickly tell if
 * if there is a need to find a parent and send the event to the parent.
 */</doc>
<use f='linux-5.3.1/fs/notify/mark.c' l='150' u='c' c='fsnotify_recalc_mask'/>
