<dec f='linux-5.3.1/security/selinux/ss/mls.h' l='35' type='int mls_context_to_sid(struct policydb * p, char oldc, char * scontext, struct context * context, struct sidtab * s, u32 def_sid)'/>
<def f='linux-5.3.1/security/selinux/ss/mls.c' l='234' ll='353' type='int mls_context_to_sid(struct policydb * pol, char oldc, char * scontext, struct context * context, struct sidtab * s, u32 def_sid)'/>
<use f='linux-5.3.1/security/selinux/ss/mls.c' l='374' u='c' c='mls_from_string'/>
<doc f='linux-5.3.1/security/selinux/ss/mls.c' l='218'>/*
 * Set the MLS fields in the security context structure
 * `context&apos; based on the string representation in
 * the string `scontext&apos;.
 *
 * This function modifies the string in place, inserting
 * NULL characters to terminate the MLS fields.
 *
 * If a def_sid is provided and no MLS field is present,
 * copy the MLS field of the associated default context.
 * Used for upgraded to MLS systems where objects may lack
 * MLS fields.
 *
 * Policy read-lock must be held for sidtab lookup.
 *
 */</doc>
<use f='linux-5.3.1/security/selinux/ss/services.c' l='1437' u='c' c='string_to_context_struct'/>
