<dec f='linux-5.3.1/drivers/gpu/drm/i915/gt/intel_mocs.h' l='56' type='int intel_rcs_context_init_mocs(struct i915_request * rq)'/>
<use f='linux-5.3.1/drivers/gpu/drm/i915/gt/intel_lrc.c' l='2564' u='c' c='gen8_init_rcs_context'/>
<def f='linux-5.3.1/drivers/gpu/drm/i915/gt/intel_mocs.c' l='556' ll='574' type='int intel_rcs_context_init_mocs(struct i915_request * rq)'/>
<doc f='linux-5.3.1/drivers/gpu/drm/i915/gt/intel_mocs.c' l='540'>/**
 * intel_rcs_context_init_mocs() - program the MOCS register.
 * @rq:	Request to set up the MOCS tables for.
 *
 * This function will emit a batch buffer with the values required for
 * programming the MOCS register values for all the currently supported
 * rings.
 *
 * These registers are partially stored in the RCS context, so they are
 * emitted at the same time so that when a context is created these registers
 * are set up. These registers have to be emitted into the start of the
 * context as setting the ELSP will re-init some of these registers back
 * to the hw values.
 *
 * Return: 0 on success, otherwise the error status.
 */</doc>
