<def f='linux-5.3.1/include/linux/crash_dump.h' l='77' ll='80' type='int is_vmcore_usable()'/>
<doc f='linux-5.3.1/include/linux/crash_dump.h' l='69'>/* is_vmcore_usable() checks if the kernel is booting after a panic and
 * the vmcore region is usable.
 *
 * This makes use of the fact that due to alignment -2ULL is not
 * a valid pointer, much in the vain of IS_ERR(), except
 * dealing directly with an unsigned long long rather than a pointer.
 */</doc>
<use f='linux-5.3.1/fs/proc/vmcore.c' l='1548' u='c' c='vmcore_init'/>
