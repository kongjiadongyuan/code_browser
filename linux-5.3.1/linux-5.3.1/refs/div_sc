<def f='linux-5.3.1/include/linux/clockchips.h' l='171' ll='179' type='unsigned long div_sc(unsigned long ticks, unsigned long nsec, int shift)'/>
<use f='linux-5.3.1/arch/x86/kernel/apic/apic.c' l='812' u='c' c='lapic_init_clockevent'/>
<doc f='linux-5.3.1/include/linux/clockchips.h' l='160'>/*
 * Calculate a multiplication factor for scaled math, which is used to convert
 * nanoseconds based values to clock ticks:
 *
 * clock_ticks = (nanoseconds * factor) &gt;&gt; shift.
 *
 * div_sc is the rearranged equation to calculate a factor from a given clock
 * ticks / nanoseconds ratio:
 *
 * factor = (clock_ticks &lt;&lt; shift) / nanoseconds
 */</doc>
