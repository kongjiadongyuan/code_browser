<def f='linux-5.3.1/kernel/sched/stats.h' l='174' ll='186' type='void sched_info_arrive(struct rq * rq, struct task_struct * t)'/>
<use f='linux-5.3.1/kernel/sched/stats.h' l='236' u='c' c='__sched_info_switch'/>
<doc f='linux-5.3.1/kernel/sched/stats.h' l='169'>/*
 * Called when a task finally hits the CPU.  We can now calculate how
 * long it was waiting to run.  We also note when it began so that we
 * can keep stats on how long its timeslice is.
 */</doc>
