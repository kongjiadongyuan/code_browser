<dec f='linux-5.3.1/include/drm/drm_agpsupport.h' l='41' type='void drm_legacy_agp_clear(struct drm_device * dev)'/>
<def f='linux-5.3.1/drivers/gpu/drm/drm_agpsupport.c' l='445' ll='467' type='void drm_legacy_agp_clear(struct drm_device * dev)'/>
<doc f='linux-5.3.1/drivers/gpu/drm/drm_agpsupport.c' l='435'>/**
 * drm_legacy_agp_clear - Clear AGP resource list
 * @dev: DRM device
 *
 * Iterate over all AGP resources and remove them. But keep the AGP head
 * intact so it can still be used. It is safe to call this if AGP is disabled or
 * was already removed.
 *
 * Cleanup is only done for drivers who have DRIVER_LEGACY set.
 */</doc>
<use f='linux-5.3.1/drivers/gpu/drm/drm_pci.c' l='220' u='c' c='drm_pci_agp_destroy'/>
