<def f='linux-5.3.1/include/crypto/hash.h' l='857' ll='860' type='int crypto_shash_export(struct shash_desc * desc, void * out)'/>
<doc f='linux-5.3.1/include/crypto/hash.h' l='845'>/**
 * crypto_shash_export() - extract operational state for message digest
 * @desc: reference to the operational state handle whose state is exported
 * @out: output buffer of sufficient size that can hold the hash state
 *
 * This function exports the hash state of the operational state handle into the
 * caller-allocated output buffer out which must have sufficient size (e.g. by
 * calling crypto_shash_descsize).
 *
 * Context: Any context.
 * Return: 0 if the export creation was successful; &lt; 0 if an error occurred
 */</doc>
<use f='linux-5.3.1/crypto/hmac.c' l='77' u='c' c='hmac_setkey'/>
<use f='linux-5.3.1/crypto/hmac.c' l='80' u='c' c='hmac_setkey'/>
<use f='linux-5.3.1/crypto/hmac.c' l='87' u='c' c='hmac_export'/>
<use f='linux-5.3.1/crypto/shash.c' l='330' u='c' c='shash_async_export'/>
