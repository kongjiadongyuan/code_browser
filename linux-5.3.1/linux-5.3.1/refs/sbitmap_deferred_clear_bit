<def f='linux-5.3.1/include/linux/sbitmap.h' l='325' ll='330' type='void sbitmap_deferred_clear_bit(struct sbitmap * sb, unsigned int bitnr)'/>
<doc f='linux-5.3.1/include/linux/sbitmap.h' l='319'>/*
 * This one is special, since it doesn&apos;t actually clear the bit, rather it
 * sets the corresponding bit in the -&gt;cleared mask instead. Paired with
 * the caller doing sbitmap_deferred_clear() if a given index is full, which
 * will clear the previously freed entries in the corresponding -&gt;word.
 */</doc>
<use f='linux-5.3.1/lib/sbitmap.c' l='590' u='c' c='sbitmap_queue_clear'/>
