<def f='linux-5.3.1/include/linux/suspend.h' l='288' ll='291' type='bool pm_suspend_no_platform()'/>
<doc f='linux-5.3.1/include/linux/suspend.h' l='277'>/**
 * pm_suspend_no_platform - Check if platform may change device power states.
 *
 * To be called during system-wide power management transitions to sleep states
 * or during the subsequent system-wide transitions back to the working state.
 *
 * Return &apos;true&apos; if the power states of devices remain under full control of the
 * kernel throughout the system-wide suspend and resume cycle in progress (that
 * is, if a device is put into a certain power state during suspend, it can be
 * expected to remain in that state during resume).
 */</doc>
<use f='linux-5.3.1/drivers/pci/pci-driver.c' l='882' u='c' c='pci_pm_suspend_noirq'/>
<use f='linux-5.3.1/drivers/pci/pci-driver.c' l='940' u='c' c='pci_pm_resume_noirq'/>
