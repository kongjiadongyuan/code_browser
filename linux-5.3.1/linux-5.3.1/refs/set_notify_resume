<def f='linux-5.3.1/include/linux/tracehook.h' l='158' ll='164' type='void set_notify_resume(struct task_struct * task)'/>
<doc f='linux-5.3.1/include/linux/tracehook.h' l='149'>/**
 * set_notify_resume - cause tracehook_notify_resume() to be called
 * @task:		task that will call tracehook_notify_resume()
 *
 * Calling this arranges that @task will call tracehook_notify_resume()
 * before returning to user mode.  If it&apos;s already running in user mode,
 * it will enter the kernel and call tracehook_notify_resume() soon.
 * If it&apos;s blocked, it will not be woken.
 */</doc>
<use f='linux-5.3.1/kernel/task_work.c' l='40' u='c' c='task_work_add'/>
