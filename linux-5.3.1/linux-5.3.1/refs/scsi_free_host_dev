<dec f='linux-5.3.1/include/scsi/scsi_host.h' l='771' type='void scsi_free_host_dev(struct scsi_device * )'/>
<doc f='linux-5.3.1/include/scsi/scsi_host.h' l='764'>/*
 * These two functions are used to allocate and free a pseudo device
 * which will connect to the host adapter itself rather than any
 * physical device.  You must deallocate when you are done with the
 * thing.  This physical pseudo-device isn&apos;t real and won&apos;t be available
 * from any high-level drivers.
 */</doc>
<def f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1927' ll='1932' type='void scsi_free_host_dev(struct scsi_device * sdev)'/>
<dec f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1933' type='void scsi_free_host_dev(struct scsi_device * )'/>
<use f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1933' c='scsi_free_host_dev'/>
<use f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1933' u='a'/>
<doc f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1919'>/**
 * scsi_free_host_dev - Free a scsi_device that points to the host adapter itself
 * @sdev: Host device to be freed
 *
 * Lock status: None assumed.
 *
 * Returns:     Nothing
 */</doc>
