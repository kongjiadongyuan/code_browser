<dec f='linux-5.3.1/include/scsi/scsi_host.h' l='772' type='struct scsi_device * scsi_get_host_dev(struct Scsi_Host * )'/>
<def f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1895' ll='1916' type='struct scsi_device * scsi_get_host_dev(struct Scsi_Host * shost)'/>
<dec f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1917' type='struct scsi_device * scsi_get_host_dev(struct Scsi_Host * )'/>
<use f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1917' c='scsi_get_host_dev'/>
<use f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1917' u='a'/>
<doc f='linux-5.3.1/drivers/scsi/scsi_scan.c' l='1878'>/**
 * scsi_get_host_dev - Create a scsi_device that points to the host adapter itself
 * @shost: Host that needs a scsi_device
 *
 * Lock status: None assumed.
 *
 * Returns:     The scsi_device or NULL
 *
 * Notes:
 *	Attach a single scsi_device to the Scsi_Host - this should
 *	be made to look like a &quot;pseudo-device&quot; that points to the
 *	HA itself.
 *
 *	Note - this device is not accessible from any high-level
 *	drivers (including generics), which is probably not
 *	optimal.  We can add hooks later to attach.
 */</doc>
<use f='linux-5.3.1/drivers/usb/storage/sierra_ms.c' l='139' u='c' c='sierra_ms_init'/>
