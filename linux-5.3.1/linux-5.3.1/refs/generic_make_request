<dec f='linux-5.3.1/include/linux/blkdev.h' l='833' type='blk_qc_t generic_make_request(struct bio * bio)'/>
<use f='linux-5.3.1/block/bio.c' l='350' u='c' c='bio_alloc_rescue'/>
<def f='linux-5.3.1/block/blk-core.c' l='994' ll='1083' type='blk_qc_t generic_make_request(struct bio * bio)'/>
<dec f='linux-5.3.1/block/blk-core.c' l='1084' type='blk_qc_t generic_make_request(struct bio * )'/>
<use f='linux-5.3.1/block/blk-core.c' l='1084' c='generic_make_request'/>
<use f='linux-5.3.1/block/blk-core.c' l='1084' u='a'/>
<use f='linux-5.3.1/block/blk-core.c' l='1163' u='c' c='submit_bio'/>
<doc f='linux-5.3.1/block/blk-core.c' l='970'>/**
 * generic_make_request - hand a buffer to its device driver for I/O
 * @bio:  The bio describing the location in memory and on the device.
 *
 * generic_make_request() is used to make I/O requests of block
 * devices. It is passed a &amp;struct bio, which describes the I/O that needs
 * to be done.
 *
 * generic_make_request() does not return any status.  The
 * success/failure status of the request, along with notification of
 * completion, is delivered asynchronously through the bio-&gt;bi_end_io
 * function described (one day) else where.
 *
 * The caller of generic_make_request must make sure that bi_io_vec
 * are set to describe the memory buffer, and that bi_dev and bi_sector are
 * set to describe the device address, and the
 * bi_end_io and optionally bi_private are set to describe how
 * completion notification should be signaled.
 *
 * generic_make_request and the drivers it calls may use bi_next if this
 * bio happens to be merged with someone else, and may resubmit the bio to
 * a lower device by calling into generic_make_request recursively, which
 * means the bio should NOT be touched after the call to -&gt;make_request_fn.
 */</doc>
<use f='linux-5.3.1/block/blk-merge.c' l='292' u='c' c='__blk_queue_split'/>
<use f='linux-5.3.1/block/bounce.c' l='310' u='c' c='__blk_queue_bounce'/>
<use f='linux-5.3.1/drivers/md/dm-raid1.c' l='782' u='c' c='do_writes'/>
<use f='linux-5.3.1/drivers/md/dm.c' l='1294' u='c' c='__map_bio'/>
<use f='linux-5.3.1/drivers/md/dm.c' l='1666' u='c' c='__split_and_process_bio'/>
<use f='linux-5.3.1/drivers/md/dm.c' l='1733' u='c' c='dm_queue_split'/>
<use f='linux-5.3.1/drivers/md/dm.c' l='2472' u='c' c='dm_wq_work'/>
