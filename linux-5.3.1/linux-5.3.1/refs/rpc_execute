<dec f='linux-5.3.1/include/linux/sunrpc/sched.h' l='227' type='void rpc_execute(struct rpc_task * )'/>
<use f='linux-5.3.1/net/sunrpc/clnt.c' l='1133' u='c' c='rpc_run_task'/>
<def f='linux-5.3.1/net/sunrpc/sched.c' l='990' ll='998' type='void rpc_execute(struct rpc_task * task)'/>
<doc f='linux-5.3.1/net/sunrpc/sched.c' l='981'>/*
 * User-visible entry point to the scheduler.
 *
 * This may be called recursively if e.g. an async NFS task updates
 * the attributes and finds that dirty pages must be flushed.
 * NOTE: Upon exit of this function the task is guaranteed to be
 *	 released. In particular note that tk_release() will have
 *	 been called, so your task memory may have been freed.
 */</doc>
