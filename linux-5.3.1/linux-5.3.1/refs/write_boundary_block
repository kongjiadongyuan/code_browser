<dec f='linux-5.3.1/include/linux/buffer_head.h' l='204' type='void write_boundary_block(struct block_device * bdev, sector_t bblock, unsigned int blocksize)'/>
<def f='linux-5.3.1/fs/buffer.c' l='534' ll='543' type='void write_boundary_block(struct block_device * bdev, sector_t bblock, unsigned int blocksize)'/>
<doc f='linux-5.3.1/fs/buffer.c' l='528'>/*
 * Called when we&apos;ve recently written block `bblock&apos;, and it is known that
 * `bblock&apos; was for a buffer_boundary() buffer.  This means that the block at
 * `bblock + 1&apos; is probably a dirty indirect block.  Hunt it down and, if it&apos;s
 * dirty, schedule it for IO.  So that indirects merge nicely with their data.
 */</doc>
<use f='linux-5.3.1/fs/mpage.c' l='665' u='c' c='__mpage_writepage'/>
