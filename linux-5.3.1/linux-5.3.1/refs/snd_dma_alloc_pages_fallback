<dec f='linux-5.3.1/include/sound/memalloc.h' l='135' type='int snd_dma_alloc_pages_fallback(int type, struct device * dev, size_t size, struct snd_dma_buffer * dmab)'/>
<def f='linux-5.3.1/sound/core/memalloc.c' l='186' ll='202' type='int snd_dma_alloc_pages_fallback(int type, struct device * device, size_t size, struct snd_dma_buffer * dmab)'/>
<dec f='linux-5.3.1/sound/core/memalloc.c' l='203' type='int snd_dma_alloc_pages_fallback(int , struct device * , size_t , struct snd_dma_buffer * )'/>
<use f='linux-5.3.1/sound/core/memalloc.c' l='203' c='snd_dma_alloc_pages_fallback'/>
<use f='linux-5.3.1/sound/core/memalloc.c' l='203' u='a'/>
<doc f='linux-5.3.1/sound/core/memalloc.c' l='171'>/**
 * snd_dma_alloc_pages_fallback - allocate the buffer area according to the given type with fallback
 * @type: the DMA buffer type
 * @device: the device pointer
 * @size: the buffer size to allocate
 * @dmab: buffer allocation record to store the allocated data
 *
 * Calls the memory-allocator function for the corresponding
 * buffer type.  When no space is left, this function reduces the size and
 * tries to allocate again.  The size actually allocated is stored in
 * res_size argument.
 *
 * Return: Zero if the buffer with the given size is allocated successfully,
 * otherwise a negative value on error.
 */</doc>
<use f='linux-5.3.1/sound/core/sgbuf.c' l='98' u='c' c='snd_malloc_sgbuf_pages'/>
