<dec f='linux-5.3.1/arch/x86/include/asm/traps.h' l='116' type='void smp_apic_timer_interrupt(struct pt_regs * regs)'/>
<def f='linux-5.3.1/arch/x86/kernel/apic/apic.c' l='1121' ll='1140' type='void smp_apic_timer_interrupt(struct pt_regs * regs)'/>
<doc f='linux-5.3.1/arch/x86/kernel/apic/apic.c' l='1113'>/*
 * Local APIC timer interrupt. This is the most natural way for doing
 * local interrupts, but local timer interrupts can be emulated by
 * broadcast interrupts too. [in case the hw doesn&apos;t support APIC timers]
 *
 * [ if a single-CPU system runs an SMP kernel then we call the local
 *   interrupt as well. Thus we cannot inline the local irq ... ]
 */</doc>
