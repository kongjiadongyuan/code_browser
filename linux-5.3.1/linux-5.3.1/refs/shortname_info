<def f='linux-5.3.1/fs/fat/namei_vfat.c' l='266' ll='270'/>
<use f='linux-5.3.1/fs/fat/namei_vfat.c' l='279' c='to_shortname_char'/>
<use f='linux-5.3.1/fs/fat/namei_vfat.c' l='338' c='vfat_create_shortname'/>
<use f='linux-5.3.1/fs/fat/namei_vfat.c' l='338' c='vfat_create_shortname'/>
<size>1</size>
<doc f='linux-5.3.1/fs/fat/namei_vfat.c' l='246'>/*
 * 1) Valid characters for the 8.3 format alias are any combination of
 * letters, uppercase alphabets, digits, any of the
 * following special characters:
 *     $ % &apos; ` - @ { } ~ ! # ( ) &amp; _ ^
 * In this case Longfilename is not stored in disk.
 *
 * WinNT&apos;s Extension:
 * File name and extension name is contain uppercase/lowercase
 * only. And it is expressed by CASE_LOWER_BASE and CASE_LOWER_EXT.
 *
 * 2) File name is 8.3 format, but it contain the uppercase and
 * lowercase char, muliti bytes char, etc. In this case numtail is not
 * added, but Longfilename is stored.
 *
 * 3) When the one except for the above, or the following special
 * character are contained:
 *        .   [ ] ; , + =
 * numtail is added, and Longfilename must be stored in disk .
 */</doc>
