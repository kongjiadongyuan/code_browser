<def f='linux-5.3.1/include/drm/drm_plane.h' l='517' ll='544'/>
<use f='linux-5.3.1/include/drm/drm_plane.h' l='639'/>
<use f='linux-5.3.1/include/drm/drm_plane.h' l='720' c='drm_universal_plane_init'/>
<doc f='linux-5.3.1/include/drm/drm_plane.h' l='499'>/**
 * enum drm_plane_type - uapi plane type enumeration
 *
 * For historical reasons not all planes are made the same. This enumeration is
 * used to tell the different types of planes apart to implement the different
 * uapi semantics for them. For userspace which is universal plane aware and
 * which is using that atomic IOCTL there&apos;s no difference between these planes
 * (beyong what the driver and hardware can support of course).
 *
 * For compatibility with legacy userspace, only overlay planes are made
 * available to userspace by default. Userspace clients may set the
 * DRM_CLIENT_CAP_UNIVERSAL_PLANES client capability bit to indicate that they
 * wish to receive a universal plane list containing all plane types. See also
 * drm_for_each_legacy_plane().
 *
 * WARNING: The values of this enum is UABI since they&apos;re exposed in the &quot;type&quot;
 * property.
 */</doc>
<use f='linux-5.3.1/drivers/gpu/drm/drm_plane.c' l='178' c='drm_universal_plane_init'/>
<use f='linux-5.3.1/drivers/gpu/drm/drm_plane.c' l='338' c='drm_plane_init'/>
<use f='linux-5.3.1/drivers/gpu/drm/i915/display/intel_sprite.c' l='2242' c='skl_universal_plane_create'/>
<use f='linux-5.3.1/drivers/gpu/drm/i915/i915_debugfs.c' l='2700' c='plane_type'/>
