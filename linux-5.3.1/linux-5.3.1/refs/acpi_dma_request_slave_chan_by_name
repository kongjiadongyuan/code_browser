<dec f='linux-5.3.1/include/linux/acpi_dma.h' l='72' type='struct dma_chan * acpi_dma_request_slave_chan_by_name(struct device * dev, const char * name)'/>
<def f='linux-5.3.1/drivers/dma/acpi-dma.c' l='423' ll='440' type='struct dma_chan * acpi_dma_request_slave_chan_by_name(struct device * dev, const char * name)'/>
<dec f='linux-5.3.1/drivers/dma/acpi-dma.c' l='441' type='struct dma_chan * acpi_dma_request_slave_chan_by_name(struct device * , const char * )'/>
<use f='linux-5.3.1/drivers/dma/acpi-dma.c' l='441' c='acpi_dma_request_slave_chan_by_name'/>
<use f='linux-5.3.1/drivers/dma/acpi-dma.c' l='441' u='a'/>
<doc f='linux-5.3.1/drivers/dma/acpi-dma.c' l='407'>/**
 * acpi_dma_request_slave_chan_by_name - Get the DMA slave channel
 * @dev:	struct device to get DMA request from
 * @name:	represents corresponding FixedDMA descriptor for @dev
 *
 * In order to support both Device Tree and ACPI in a single driver we
 * translate the names &quot;tx&quot; and &quot;rx&quot; here based on the most common case where
 * the first FixedDMA descriptor is TX and second is RX.
 *
 * If the device has &quot;dma-names&quot; property the FixedDMA descriptor indices
 * are retrieved based on those. Otherwise the function falls back using
 * hardcoded indices.
 *
 * Return:
 * Pointer to appropriate dma channel on success or an error pointer.
 */</doc>
<use f='linux-5.3.1/drivers/dma/dmaengine.c' l='705' u='c' c='dma_request_chan'/>
