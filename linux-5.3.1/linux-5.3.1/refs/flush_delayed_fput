<dec f='linux-5.3.1/include/linux/file.h' l='91' type='void flush_delayed_fput()'/>
<def f='linux-5.3.1/fs/file_table.c' l='326' ll='329' type='void flush_delayed_fput()'/>
<doc f='linux-5.3.1/fs/file_table.c' l='316'>/*
 * If kernel thread really needs to have the final fput() it has done
 * to complete, call this.  The only user right now is the boot - we
 * *do* need to make sure our writes to binaries on initramfs has
 * not left us with opened struct file waiting for __fput() - execve()
 * won&apos;t work without that.  Please, don&apos;t add more callers without
 * very good reasons; in particular, never call that with locks
 * held and never call that from a thread that might need to do
 * some work on any kind of umount.
 */</doc>
<use f='linux-5.3.1/init/initramfs.c' l='677' u='c' c='populate_rootfs'/>
