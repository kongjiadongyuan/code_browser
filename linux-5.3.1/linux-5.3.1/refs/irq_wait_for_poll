<dec f='linux-5.3.1/kernel/irq/internals.h' l='112' type='bool irq_wait_for_poll(struct irq_desc * desc)'/>
<use f='linux-5.3.1/kernel/irq/chip.c' l='499' u='c' c='irq_check_poll'/>
<def f='linux-5.3.1/kernel/irq/spurious.c' l='36' ll='55' type='bool irq_wait_for_poll(struct irq_desc * desc)'/>
<doc f='linux-5.3.1/kernel/irq/spurious.c' l='25'>/*
 * We wait here for a poller to finish.
 *
 * If the poll runs on this CPU, then we yell loudly and return
 * false. That will leave the interrupt line disabled in the worst
 * case, but it should never happen.
 *
 * We wait until the poller is done and then recheck disabled and
 * action (about to be disabled). Only if it&apos;s still active, we return
 * true and let the handler run.
 */</doc>
