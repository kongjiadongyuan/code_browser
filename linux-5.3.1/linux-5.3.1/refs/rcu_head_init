<def f='linux-5.3.1/include/linux/rcupdate.h' l='861' ll='864' type='void rcu_head_init(struct callback_head * rhp)'/>
<doc f='linux-5.3.1/include/linux/rcupdate.h' l='851'>/**
 * rcu_head_init - Initialize rcu_head for rcu_head_after_call_rcu()
 * @rhp: The rcu_head structure to initialize.
 *
 * If you intend to invoke rcu_head_after_call_rcu() to test whether a
 * given rcu_head structure has already been passed to call_rcu(), then
 * you must also invoke this rcu_head_init() function on it just after
 * allocating that structure.  Calls to this function must not race with
 * calls to call_rcu(), rcu_head_after_call_rcu(), or callback invocation.
 */</doc>
<use f='linux-5.3.1/lib/rhashtable.c' l='191' u='c' c='bucket_table_alloc'/>
