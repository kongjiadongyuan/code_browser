<dec f='linux-5.3.1/include/linux/device.h' l='346' type='int driver_deferred_probe_check_state(struct device * dev)'/>
<def f='linux-5.3.1/drivers/base/dd.c' l='262' ll='273' type='int driver_deferred_probe_check_state(struct device * dev)'/>
<doc f='linux-5.3.1/drivers/base/dd.c' l='251'>/**
 * driver_deferred_probe_check_state() - Check deferred probe state
 * @dev: device to check
 *
 * Returns -ENODEV if init is done and all built-in drivers have had a chance
 * to probe (i.e. initcalls are done), -ETIMEDOUT if deferred probe debug
 * timeout has expired, or -EPROBE_DEFER if none of those conditions are met.
 *
 * Drivers or subsystems can opt-in to calling this function instead of directly
 * returning -EPROBE_DEFER.
 */</doc>
