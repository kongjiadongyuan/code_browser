<dec f='linux-5.3.1/include/linux/acpi.h' l='453' type='struct acpi_device * acpi_resource_consumer(struct resource * res)'/>
<def f='linux-5.3.1/drivers/acpi/resource.c' l='770' ll='776' type='struct acpi_device * acpi_resource_consumer(struct resource * res)'/>
<doc f='linux-5.3.1/drivers/acpi/resource.c' l='762'>/**
 * acpi_resource_consumer - Find the ACPI device that consumes @res.
 * @res: Resource to search for.
 *
 * Search the current resource settings (_CRS) of every ACPI device node
 * for @res.  If we find an ACPI device whose _CRS includes @res, return
 * it.  Otherwise, return NULL.
 */</doc>
