<dec f='linux-5.3.1/include/net/ipv6.h' l='433' type='struct ipv6_txoptions * ipv6_renew_options(struct sock * sk, struct ipv6_txoptions * opt, int newtype, struct ipv6_opt_hdr * newopt)'/>
<use f='linux-5.3.1/net/ipv6/calipso.c' l='788' u='c' c='calipso_opt_update'/>
<use f='linux-5.3.1/net/ipv6/calipso.c' l='1210' u='c' c='calipso_req_setattr'/>
<use f='linux-5.3.1/net/ipv6/calipso.c' l='1247' u='c' c='calipso_req_delattr'/>
<def f='linux-5.3.1/net/ipv6/exthdrs.c' l='1052' ll='1107' type='struct ipv6_txoptions * ipv6_renew_options(struct sock * sk, struct ipv6_txoptions * opt, int newtype, struct ipv6_opt_hdr * newopt)'/>
<doc f='linux-5.3.1/net/ipv6/exthdrs.c' l='1031'>/**
 * ipv6_renew_options - replace a specific ext hdr with a new one.
 *
 * @sk: sock from which to allocate memory
 * @opt: original options
 * @newtype: option type to replace in @opt
 * @newopt: new option of type @newtype to replace (user-mem)
 * @newoptlen: length of @newopt
 *
 * Returns a new set of options which is a copy of @opt with the
 * option type @newtype replaced with @newopt.
 *
 * @opt may be NULL, in which case a new set of options is returned
 * containing just @newopt.
 *
 * @newopt may be NULL, in which case the specified option type is
 * not copied into the new set of options.
 *
 * The new set of options is allocated from the socket option memory
 * buffer of @sk.
 */</doc>
<use f='linux-5.3.1/net/ipv6/ipv6_sockglue.c' l='430' u='c' c='do_ipv6_setsockopt'/>
