<dec f='linux-5.3.1/include/acpi/acpi_bus.h' l='591' type='int acpi_dma_get_range(struct device * dev, u64 * dma_addr, u64 * offset, u64 * size)'/>
<def f='linux-5.3.1/drivers/acpi/scan.c' l='1382' ll='1445' type='int acpi_dma_get_range(struct device * dev, u64 * dma_addr, u64 * offset, u64 * size)'/>
<doc f='linux-5.3.1/drivers/acpi/scan.c' l='1368'>/**
 * acpi_dma_get_range() - Get device DMA parameters.
 *
 * @dev: device to configure
 * @dma_addr: pointer device DMA address result
 * @offset: pointer to the DMA offset result
 * @size: pointer to DMA range size result
 *
 * Evaluate DMA regions and return respectively DMA region start, offset
 * and size in dma_addr, offset and size on parsing success; it does not
 * update the passed in values on failure.
 *
 * Return 0 on success, &lt; 0 on failure.
 */</doc>
