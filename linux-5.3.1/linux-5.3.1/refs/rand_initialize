<dec f='linux-5.3.1/include/linux/random.h' l='39' type='int rand_initialize()'/>
<def f='linux-5.3.1/drivers/char/random.c' l='1895' ll='1906' type='int rand_initialize()'/>
<doc f='linux-5.3.1/drivers/char/random.c' l='1885'>/*
 * Note that setup_arch() may call add_device_randomness()
 * long before we get here. This allows seeding of the pools
 * with some platform dependent data very early in the boot
 * process. But it limits our options here. We must use
 * statically allocated structures that already have all
 * initializations complete at compile time. We should also
 * take care not to overwrite the precious per platform data
 * we were given.
 */</doc>
<use f='linux-5.3.1/init/main.c' l='690' u='c' c='start_kernel'/>
