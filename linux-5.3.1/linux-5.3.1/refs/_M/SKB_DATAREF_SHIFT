<dec f='linux-5.3.1/include/linux/skbuff.h' l='552'/>
<use f='linux-5.3.1/include/linux/skbuff.h' l='1621' u='c'/>
<use f='linux-5.3.1/include/linux/skbuff.h' l='1642' u='c'/>
<doc f='linux-5.3.1/include/linux/skbuff.h' l='541'>/* We divide dataref into two halves.  The higher 16 bits hold references
 * to the payload part of skb-&gt;data.  The lower 16 bits hold references to
 * the entire skb-&gt;data.  A clone of a headerless skb holds the length of
 * the header in skb-&gt;hdr_len.
 *
 * All users must obey the rule that the skb-&gt;data reference count must be
 * greater than or equal to the payload reference count.
 *
 * Holding a reference to the payload part means that the user does not
 * care about modifications to the header part of skb-&gt;data.
 */</doc>
<use f='linux-5.3.1/net/core/skbuff.c' l='600' u='c'/>
