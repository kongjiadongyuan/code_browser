<dec f='linux-5.3.1/include/linux/kernel.h' l='62'/>
<doc f='linux-5.3.1/include/linux/kernel.h' l='56'>/*
 * This looks more complex than it should be. But we need to
 * get the type for the ~ right in round_down (it needs to be
 * as wide as the result!), and we want to evaluate the macro
 * arguments just once each.
 */</doc>
<dec f='linux-5.3.1/tools/include/linux/kernel.h' l='115'/>
<doc f='linux-5.3.1/tools/include/linux/kernel.h' l='109'>/*
 * This looks more complex than it should be. But we need to
 * get the type for the ~ right in round_down (it needs to be
 * as wide as the result!), and we want to evaluate the macro
 * arguments just once each.
 */</doc>
