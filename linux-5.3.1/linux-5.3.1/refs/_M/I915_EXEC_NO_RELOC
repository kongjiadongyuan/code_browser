<dec f='linux-5.3.1/include/uapi/drm/i915_drm.h' l='1073'/>
<doc f='linux-5.3.1/include/uapi/drm/i915_drm.h' l='1068'>/** Provide a hint to the kernel that the command stream and auxiliary
 * state buffers already holds the correct presumed addresses and so the
 * relocation process may be skipped if no buffers need to be moved in
 * preparation for the execbuffer.
 */</doc>
<use f='linux-5.3.1/drivers/gpu/drm/i915/gem/i915_gem_execbuffer.c' l='2368' u='c'/>
