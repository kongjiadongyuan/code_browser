<dec f='linux-5.3.1/include/uapi/linux/falloc.h' l='43'/>
<doc f='linux-5.3.1/include/uapi/linux/falloc.h' l='31'>/*
 * FALLOC_FL_ZERO_RANGE is used to convert a range of file to zeros preferably
 * without issuing data IO. Blocks should be preallocated for the regions that
 * span holes in the file, and the entire range is preferable converted to
 * unwritten extents - even though file system may choose to zero out the
 * extent or do whatever which will result in reading zeros from the range
 * while the range remains allocated for the file.
 *
 * This can be also used to preallocate blocks past EOF in the same way as
 * with fallocate. Flag FALLOC_FL_KEEP_SIZE should cause the inode
 * size to remain the same.
 */</doc>
<use f='linux-5.3.1/fs/block_dev.c' l='2084' u='c'/>
<use f='linux-5.3.1/fs/block_dev.c' l='2085' u='c'/>
<use f='linux-5.3.1/fs/ext4/extents.c' l='4833' u='c'/>
<use f='linux-5.3.1/fs/ext4/extents.c' l='4838' u='c'/>
<use f='linux-5.3.1/fs/ext4/extents.c' l='4855' u='c'/>
<use f='linux-5.3.1/fs/open.c' l='242' u='c'/>
<use f='linux-5.3.1/fs/open.c' l='243' u='c'/>
<dec f='include/linux/falloc.h' l='43'/>
<doc f='include/linux/falloc.h' l='31'>/*
 * FALLOC_FL_ZERO_RANGE is used to convert a range of file to zeros preferably
 * without issuing data IO. Blocks should be preallocated for the regions that
 * span holes in the file, and the entire range is preferable converted to
 * unwritten extents - even though file system may choose to zero out the
 * extent or do whatever which will result in reading zeros from the range
 * while the range remains allocated for the file.
 *
 * This can be also used to preallocate blocks past EOF in the same way as
 * with fallocate. Flag FALLOC_FL_KEEP_SIZE should cause the inode
 * size to remain the same.
 */</doc>
