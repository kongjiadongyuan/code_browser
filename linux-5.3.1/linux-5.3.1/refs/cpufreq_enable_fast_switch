<dec f='linux-5.3.1/include/linux/cpufreq.h' l='208' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * policy)'/>
<def f='linux-5.3.1/drivers/cpufreq/cpufreq.c' l='480' ll='497' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * policy)'/>
<dec f='linux-5.3.1/drivers/cpufreq/cpufreq.c' l='498' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * )'/>
<use f='linux-5.3.1/drivers/cpufreq/cpufreq.c' l='498' c='cpufreq_enable_fast_switch'/>
<use f='linux-5.3.1/drivers/cpufreq/cpufreq.c' l='498' u='a'/>
<doc f='linux-5.3.1/drivers/cpufreq/cpufreq.c' l='469'>/**
 * cpufreq_enable_fast_switch - Enable fast frequency switching for policy.
 * @policy: cpufreq policy to enable fast frequency switching for.
 *
 * Try to enable fast frequency switching for @policy.
 *
 * The attempt will fail if there is at least one transition notifier registered
 * at this point, as fast frequency switching is quite fundamentally at odds
 * with transition notifiers.  Thus if successful, it will make registration of
 * transition notifiers fail going forward.
 */</doc>
