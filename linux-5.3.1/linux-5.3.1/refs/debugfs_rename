<dec f='linux-5.3.1/include/linux/debugfs.h' l='97' type='struct dentry * debugfs_rename(struct dentry * old_dir, struct dentry * old_dentry, struct dentry * new_dir, const char * new_name)'/>
<def f='linux-5.3.1/fs/debugfs/inode.c' l='806' ll='856' type='struct dentry * debugfs_rename(struct dentry * old_dir, struct dentry * old_dentry, struct dentry * new_dir, const char * new_name)'/>
<dec f='linux-5.3.1/fs/debugfs/inode.c' l='857' type='struct dentry * debugfs_rename(struct dentry * , struct dentry * , struct dentry * , const char * )'/>
<use f='linux-5.3.1/fs/debugfs/inode.c' l='857' c='debugfs_rename'/>
<use f='linux-5.3.1/fs/debugfs/inode.c' l='857' u='a'/>
<doc f='linux-5.3.1/fs/debugfs/inode.c' l='787'>/**
 * debugfs_rename - rename a file/directory in the debugfs filesystem
 * @old_dir: a pointer to the parent dentry for the renamed object. This
 *          should be a directory dentry.
 * @old_dentry: dentry of an object to be renamed.
 * @new_dir: a pointer to the parent dentry where the object should be
 *          moved. This should be a directory dentry.
 * @new_name: a pointer to a string containing the target name.
 *
 * This function renames a file/directory in debugfs.  The target must not
 * exist for rename to succeed.
 *
 * This function will return a pointer to old_dentry (which is updated to
 * reflect renaming) if it succeeds. If an error occurs, %NULL will be
 * returned.
 *
 * If debugfs is not enabled in the kernel, the value -%ENODEV will be
 * returned.
 */</doc>
<use f='linux-5.3.1/net/wireless/core.c' l='146' u='c' c='cfg80211_dev_rename'/>
