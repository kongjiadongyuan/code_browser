<def f='linux-5.3.1/include/crypto/hash.h' l='576' ll='588' type='struct ahash_request * ahash_request_alloc(struct crypto_ahash * tfm, gfp_t gfp)'/>
<doc f='linux-5.3.1/include/crypto/hash.h' l='564'>/**
 * ahash_request_alloc() - allocate request data structure
 * @tfm: cipher handle to be registered with the request
 * @gfp: memory allocation flag that is handed to kmalloc by the API call.
 *
 * Allocate the request data structure that must be used with the ahash
 * message digest API calls. During
 * the allocation, the provided ahash handle
 * is registered in the request data structure.
 *
 * Return: allocated request handle in case of success, or NULL if out of memory
 */</doc>
<use f='linux-5.3.1/net/ipv4/tcp.c' l='3723' u='c' c='__tcp_alloc_md5sig_pool'/>
