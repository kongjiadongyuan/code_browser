<def f='linux-5.3.1/include/linux/hid.h' l='944' ll='951' type='void hid_device_io_stop(struct hid_device * hid)'/>
<doc f='linux-5.3.1/include/linux/hid.h' l='933'>/**
 * hid_device_io_stop - disable HID input during probe, remove
 *
 * @hid - the device
 *
 * Should only be called after hid_device_io_start. It will prevent
 * incoming packets from going to the driver for the duration of
 * probe, remove. If called during probe, packets will still go to the
 * driver after probe is complete. This function should only be called
 * by the thread calling probe or remove.
 */</doc>
<use f='linux-5.3.1/drivers/hid/usbhid/hid-pidff.c' l='1314' u='c' c='hid_pidff_init'/>
<use f='linux-5.3.1/drivers/hid/usbhid/hid-pidff.c' l='1319' u='c' c='hid_pidff_init'/>
