<dec f='linux-5.3.1/include/linux/libata.h' l='1100' type='int ata_host_start(struct ata_host * host)'/>
<use f='linux-5.3.1/drivers/ata/libahci.c' l='2539' u='c' c='ahci_host_activate_multi_irqs'/>
<def f='linux-5.3.1/drivers/ata/libata-core.c' l='6347' ll='6409' type='int ata_host_start(struct ata_host * host)'/>
<use f='linux-5.3.1/drivers/ata/libata-core.c' l='6614' u='c' c='ata_host_activate'/>
<dec f='linux-5.3.1/drivers/ata/libata-core.c' l='7295' type='int ata_host_start(struct ata_host * )'/>
<use f='linux-5.3.1/drivers/ata/libata-core.c' l='7295' c='ata_host_start'/>
<use f='linux-5.3.1/drivers/ata/libata-core.c' l='7295' u='a'/>
<doc f='linux-5.3.1/drivers/ata/libata-core.c' l='6331'>/**
 *	ata_host_start - start and freeze ports of an ATA host
 *	@host: ATA host to start ports for
 *
 *	Start and then freeze ports of @host.  Started status is
 *	recorded in host-&gt;flags, so this function can be called
 *	multiple times.  Ports are guaranteed to get started only
 *	once.  If host-&gt;ops isn&apos;t initialized yet, its set to the
 *	first non-dummy port ops.
 *
 *	LOCKING:
 *	Inherited from calling layer (may sleep).
 *
 *	RETURNS:
 *	0 if all ports are started successfully, -errno otherwise.
 */</doc>
<use f='linux-5.3.1/drivers/ata/libata-sff.c' l='2357' u='c' c='ata_pci_sff_activate_host'/>
