<def f='linux-5.3.1/include/linux/ptr_ring.h' l='510' ll='555' type='void ptr_ring_unconsume(struct ptr_ring * r, void ** batch, int n, void (*)(void *) destroy)'/>
<doc f='linux-5.3.1/include/linux/ptr_ring.h' l='500'>/*
 * Return entries into ring. Destroy entries that don&apos;t fit.
 *
 * Note: this is expected to be a rare slow path operation.
 *
 * Note: producer lock is nested within consumer lock, so if you
 * resize you must make sure all uses nest correctly.
 * In particular if you consume ring in interrupt or BH context, you must
 * disable interrupts/BH when doing so.
 */</doc>
<use f='linux-5.3.1/include/linux/skb_array.h' l='193' u='c' c='skb_array_unconsume'/>
