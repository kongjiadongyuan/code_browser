<def f='linux-5.3.1/drivers/usb/host/uhci-q.c' l='314' ll='366' type='int uhci_cleanup_queue(struct uhci_hcd * uhci, struct uhci_qh * qh, struct urb * urb)'/>
<use f='linux-5.3.1/drivers/usb/host/uhci-q.c' l='1622' u='c' c='uhci_scan_qh'/>
<doc f='linux-5.3.1/drivers/usb/host/uhci-q.c' l='307'>/*
 * When a queue is stopped and a dequeued URB is given back, adjust
 * the previous TD link (if the URB isn&apos;t first on the queue) or
 * save its toggle value (if it is first and is currently executing).
 *
 * Returns 0 if the URB should not yet be given back, 1 otherwise.
 */</doc>
