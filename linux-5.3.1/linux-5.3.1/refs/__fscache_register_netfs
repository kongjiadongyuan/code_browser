<dec f='linux-5.3.1/include/linux/fscache.h' l='186' type='int __fscache_register_netfs(struct fscache_netfs * )'/>
<use f='linux-5.3.1/include/linux/fscache.h' l='243' u='c' c='fscache_register_netfs'/>
<doc f='linux-5.3.1/include/linux/fscache.h' l='179'>/*
 * slow-path functions for when there is actually caching available, and the
 * netfs does actually have a valid token
 * - these are not to be called directly
 * - these are undefined symbols when FS-Cache is not configured and the
 *   optimiser takes care of not using them
 */</doc>
