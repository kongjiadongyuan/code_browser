<def f='linux-5.3.1/include/linux/xarray.h' l='817' ll='827' type='int xa_alloc(struct xarray * xa, u32 * id, void * entry, struct xa_limit limit, gfp_t gfp)'/>
<doc f='linux-5.3.1/include/linux/xarray.h' l='800'>/**
 * xa_alloc() - Find somewhere to store this entry in the XArray.
 * @xa: XArray.
 * @id: Pointer to ID.
 * @entry: New entry.
 * @limit: Range of ID to allocate.
 * @gfp: Memory allocation flags.
 *
 * Finds an empty entry in @xa between @limit.min and @limit.max,
 * stores the index into the @id pointer, then stores the entry at
 * that index.  A concurrent lookup will not see an uninitialised @id.
 *
 * Context: Any context.  Takes and releases the xa_lock.  May sleep if
 * the @gfp flags permit.
 * Return: 0 on success, -ENOMEM if memory could not be allocated or
 * -EBUSY if there are no free entries in @limit.
 */</doc>
<use f='linux-5.3.1/drivers/gpu/drm/drm_gem.c' l='1381' u='c' c='drm_gem_fence_array_add'/>
