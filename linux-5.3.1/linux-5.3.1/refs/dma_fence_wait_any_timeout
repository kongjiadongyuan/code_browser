<dec f='linux-5.3.1/include/linux/dma-fence.h' l='520' type='long dma_fence_wait_any_timeout(struct dma_fence ** fences, uint32_t count, bool intr, long timeout, uint32_t * idx)'/>
<def f='linux-5.3.1/drivers/dma-buf/dma-fence.c' l='576' ll='642' type='long dma_fence_wait_any_timeout(struct dma_fence ** fences, uint32_t count, bool intr, long timeout, uint32_t * idx)'/>
<dec f='linux-5.3.1/drivers/dma-buf/dma-fence.c' l='643' type='long dma_fence_wait_any_timeout(struct dma_fence ** , uint32_t , bool , long , uint32_t * )'/>
<use f='linux-5.3.1/drivers/dma-buf/dma-fence.c' l='643' c='dma_fence_wait_any_timeout'/>
<use f='linux-5.3.1/drivers/dma-buf/dma-fence.c' l='643' u='a'/>
<doc f='linux-5.3.1/drivers/dma-buf/dma-fence.c' l='556'>/**
 * dma_fence_wait_any_timeout - sleep until any fence gets signaled
 * or until timeout elapses
 * @fences: array of fences to wait on
 * @count: number of fences to wait on
 * @intr: if true, do an interruptible wait
 * @timeout: timeout value in jiffies, or MAX_SCHEDULE_TIMEOUT
 * @idx: used to store the first signaled fence index, meaningful only on
 *	positive return
 *
 * Returns -EINVAL on custom fence wait implementation, -ERESTARTSYS if
 * interrupted, 0 if the wait timed out, or the remaining timeout in jiffies
 * on success.
 *
 * Synchronous waits for the first fence in the array to be signaled. The
 * caller needs to hold a reference to all fences in the array, otherwise a
 * fence might be freed before return, resulting in undefined behavior.
 *
 * See also dma_fence_wait() and dma_fence_wait_timeout().
 */</doc>
