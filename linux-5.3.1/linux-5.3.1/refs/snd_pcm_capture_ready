<def f='linux-5.3.1/include/sound/pcm.h' l='791' ll='795' type='int snd_pcm_capture_ready(struct snd_pcm_substream * substream)'/>
<doc f='linux-5.3.1/include/sound/pcm.h' l='783'>/**
 * snd_pcm_capture_ready - check whether the capture buffer is available
 * @substream: the pcm substream instance
 *
 * Checks whether enough capture data is available on the capture buffer.
 *
 * Return: Non-zero if available, or zero if not.
 */</doc>
