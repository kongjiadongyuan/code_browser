<dec f='linux-5.3.1/include/linux/irqdomain.h' l='400' type='unsigned int irq_create_direct_mapping(struct irq_domain * host)'/>
<def f='linux-5.3.1/kernel/irq/irqdomain.c' l='607' ll='635' type='unsigned int irq_create_direct_mapping(struct irq_domain * domain)'/>
<dec f='linux-5.3.1/kernel/irq/irqdomain.c' l='636' type='unsigned int irq_create_direct_mapping(struct irq_domain * )'/>
<use f='linux-5.3.1/kernel/irq/irqdomain.c' l='636' c='irq_create_direct_mapping'/>
<use f='linux-5.3.1/kernel/irq/irqdomain.c' l='636' u='a'/>
<doc f='linux-5.3.1/kernel/irq/irqdomain.c' l='597'>/**
 * irq_create_direct_mapping() - Allocate an irq for direct mapping
 * @domain: domain to allocate the irq for or NULL for default domain
 *
 * This routine is used for irq controllers which can choose the hardware
 * interrupt numbers they generate. In such a case it&apos;s simplest to use
 * the linux irq as the hardware interrupt number. It still uses the linear
 * or radix tree to store the mapping, but the irq controller can optimize
 * the revmap path by using the hwirq directly.
 */</doc>
