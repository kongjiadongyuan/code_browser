<def f='linux-5.3.1/include/crypto/kpp.h' l='353' ll='358' type='unsigned int crypto_kpp_maxsize(struct crypto_kpp * tfm)'/>
<doc f='linux-5.3.1/include/crypto/kpp.h' l='343'>/**
 * crypto_kpp_maxsize() - Get len for output buffer
 *
 * Function returns the output buffer size required for a given key.
 * Function assumes that the key is already set in the transformation. If this
 * function is called without a setkey or with a failed setkey, you will end up
 * in a NULL dereference.
 *
 * @tfm:	KPP tfm handle allocated with crypto_alloc_kpp()
 */</doc>
