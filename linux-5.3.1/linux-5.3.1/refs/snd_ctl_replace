<dec f='linux-5.3.1/include/sound/control.h' l='113' type='int snd_ctl_replace(struct snd_card * card, struct snd_kcontrol * kcontrol, bool add_on_replace)'/>
<def f='linux-5.3.1/sound/core/control.c' l='442' ll='447' type='int snd_ctl_replace(struct snd_card * card, struct snd_kcontrol * kcontrol, bool add_on_replace)'/>
<dec f='linux-5.3.1/sound/core/control.c' l='448' type='int snd_ctl_replace(struct snd_card * , struct snd_kcontrol * , bool )'/>
<use f='linux-5.3.1/sound/core/control.c' l='448' c='snd_ctl_replace'/>
<use f='linux-5.3.1/sound/core/control.c' l='448' u='a'/>
<doc f='linux-5.3.1/sound/core/control.c' l='428'>/**
 * snd_ctl_replace - replace the control instance of the card
 * @card: the card instance
 * @kcontrol: the control instance to replace
 * @add_on_replace: add the control if not already added
 *
 * Replaces the given control.  If the given control does not exist
 * and the add_on_replace flag is set, the control is added.  If the
 * control exists, it is destroyed first.
 *
 * It frees automatically the control which cannot be added or replaced.
 *
 * Return: Zero if successful, or a negative error code on failure.
 */</doc>
