<dec f='linux-5.3.1/include/linux/tty.h' l='500' type='void tty_vhangup_session(struct tty_struct * tty)'/>
<def f='linux-5.3.1/drivers/tty/tty_io.c' l='731' ll='735' type='void tty_vhangup_session(struct tty_struct * tty)'/>
<doc f='linux-5.3.1/drivers/tty/tty_io.c' l='720'>/**
 *	tty_vhangup_session		-	hangup session leader exit
 *	@tty: tty to hangup
 *
 *	The session leader is exiting and hanging up its controlling terminal.
 *	Every process in the foreground process group is signalled SIGHUP.
 *
 *	We do this synchronously so that when the syscall returns the process
 *	is complete. That guarantee is necessary for security reasons.
 */</doc>
<use f='linux-5.3.1/drivers/tty/tty_jobctrl.c' l='267' u='c' c='disassociate_ctty'/>
