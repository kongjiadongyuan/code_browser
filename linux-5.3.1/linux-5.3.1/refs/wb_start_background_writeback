<dec f='linux-5.3.1/include/linux/backing-dev.h' l='43' type='void wb_start_background_writeback(struct bdi_writeback * wb)'/>
<def f='linux-5.3.1/fs/fs-writeback.c' l='1020' ll='1028' type='void wb_start_background_writeback(struct bdi_writeback * wb)'/>
<doc f='linux-5.3.1/fs/fs-writeback.c' l='1010'>/**
 * wb_start_background_writeback - start background writeback
 * @wb: bdi_writback to write from
 *
 * Description:
 *   This makes sure WB_SYNC_NONE background writeback happens. When
 *   this function returns, it is only guaranteed that for given wb
 *   some IO is happening if we are over background dirty threshold.
 *   Caller need not hold sb s_umount semaphore.
 */</doc>
<use f='linux-5.3.1/mm/page-writeback.c' l='1668' u='c' c='balance_dirty_pages'/>
<use f='linux-5.3.1/mm/page-writeback.c' l='1828' u='c' c='balance_dirty_pages'/>
