<def f='linux-5.3.1/drivers/usb/host/uhci-pci.c' l='161' ll='166' type='void uhci_shutdown(struct pci_dev * pdev)'/>
<use f='linux-5.3.1/drivers/usb/host/uhci-pci.c' l='302'/>
<use f='linux-5.3.1/drivers/usb/host/uhci-pci.c' l='302'/>
<doc f='linux-5.3.1/drivers/usb/host/uhci-pci.c' l='154'>/* Make sure the controller is quiescent and that we&apos;re not using it
 * any more.  This is mainly for the benefit of programs which, like kexec,
 * expect the hardware to be idle: not doing DMA or generating IRQs.
 *
 * This routine may be called in a damaged or failing kernel.  Hence we
 * do not acquire the spinlock before shutting down the controller.
 */</doc>
