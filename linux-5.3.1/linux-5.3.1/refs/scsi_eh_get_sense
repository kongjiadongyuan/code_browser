<dec f='linux-5.3.1/drivers/scsi/scsi_priv.h' l='82' type='int scsi_eh_get_sense(struct list_head * work_q, struct list_head * done_q)'/>
<def f='linux-5.3.1/drivers/scsi/scsi_error.c' l='1213' ll='1276' type='int scsi_eh_get_sense(struct list_head * work_q, struct list_head * done_q)'/>
<dec f='linux-5.3.1/drivers/scsi/scsi_error.c' l='1277' type='int scsi_eh_get_sense(struct list_head * , struct list_head * )'/>
<use f='linux-5.3.1/drivers/scsi/scsi_error.c' l='1277' c='scsi_eh_get_sense'/>
<use f='linux-5.3.1/drivers/scsi/scsi_error.c' l='1277' u='a'/>
<use f='linux-5.3.1/drivers/scsi/scsi_error.c' l='2149' u='c' c='scsi_unjam_host'/>
<doc f='linux-5.3.1/drivers/scsi/scsi_error.c' l='1193'>/**
 * scsi_eh_get_sense - Get device sense data.
 * @work_q:	Queue of commands to process.
 * @done_q:	Queue of processed commands.
 *
 * Description:
 *    See if we need to request sense information.  if so, then get it
 *    now, so we have a better idea of what to do.
 *
 * Notes:
 *    This has the unfortunate side effect that if a shost adapter does
 *    not automatically request sense information, we end up shutting
 *    it down before we request it.
 *
 *    All drivers should request sense information internally these days,
 *    so for now all I have to say is tough noogies if you end up in here.
 *
 *    XXX: Long term this code should go away, but that needs an audit of
 *         all LLDDs first.
 */</doc>
