<dec f='linux-5.3.1/include/drm/drm_modes.h' l='535' type='void drm_connector_list_update(struct drm_connector * connector)'/>
<def f='linux-5.3.1/drivers/gpu/drm/drm_modes.c' l='1367' ll='1415' type='void drm_connector_list_update(struct drm_connector * connector)'/>
<dec f='linux-5.3.1/drivers/gpu/drm/drm_modes.c' l='1416' type='void drm_connector_list_update(struct drm_connector * )'/>
<use f='linux-5.3.1/drivers/gpu/drm/drm_modes.c' l='1416' c='drm_connector_list_update'/>
<use f='linux-5.3.1/drivers/gpu/drm/drm_modes.c' l='1416' u='a'/>
<doc f='linux-5.3.1/drivers/gpu/drm/drm_modes.c' l='1356'>/**
 * drm_connector_list_update - update the mode list for the connector
 * @connector: the connector to update
 *
 * This moves the modes from the @connector probed_modes list
 * to the actual mode list. It compares the probed mode against the current
 * list and only adds different/new modes.
 *
 * This is just a helper functions doesn&apos;t validate any modes itself and also
 * doesn&apos;t prune any invalid modes. Callers need to do that themselves.
 */</doc>
<use f='linux-5.3.1/drivers/gpu/drm/drm_probe_helper.c' l='496' u='c' c='drm_helper_probe_single_connector_modes'/>
