<dec f='linux-5.3.1/security/keys/internal.h' l='178' type='void key_gc_keytype(struct key_type * ktype)'/>
<def f='linux-5.3.1/security/keys/gc.c' l='99' ll='117' type='void key_gc_keytype(struct key_type * ktype)'/>
<doc f='linux-5.3.1/security/keys/gc.c' l='89'>/*
 * Reap keys of dead type.
 *
 * We use three flags to make sure we see three complete cycles of the garbage
 * collector: the first to mark keys of that type as being dead, the second to
 * collect dead links and the third to clean up the dead keys.  We have to be
 * careful as there may already be a cycle in progress.
 *
 * The caller must be holding key_types_sem.
 */</doc>
<use f='linux-5.3.1/security/keys/key.c' l='1168' u='c' c='unregister_key_type'/>
