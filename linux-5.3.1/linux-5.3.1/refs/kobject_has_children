<def f='linux-5.3.1/include/linux/kobject.h' l='132' ll='137' type='bool kobject_has_children(struct kobject * kobj)'/>
<doc f='linux-5.3.1/include/linux/kobject.h' l='122'>/**
 * kobject_has_children - Returns whether a kobject has children.
 * @kobj: the object to test
 *
 * This will return whether a kobject has other kobjects as children.
 *
 * It does NOT account for the presence of attribute files, only sub
 * directories. It also assumes there is no concurrent addition or
 * removal of such children, and thus relies on external locking.
 */</doc>
<use f='linux-5.3.1/drivers/base/core.c' l='1882' u='c' c='cleanup_glue_dir'/>
