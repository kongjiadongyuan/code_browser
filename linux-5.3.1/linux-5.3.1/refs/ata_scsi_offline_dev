<dec f='linux-5.3.1/drivers/ata/libata.h' l='118' type='int ata_scsi_offline_dev(struct ata_device * dev)'/>
<use f='linux-5.3.1/drivers/ata/libata-eh.c' l='1315' u='c' c='ata_eh_detach_dev'/>
<def f='linux-5.3.1/drivers/ata/libata-scsi.c' l='4664' ll='4671' type='int ata_scsi_offline_dev(struct ata_device * dev)'/>
<doc f='linux-5.3.1/drivers/ata/libata-scsi.c' l='4649'>/**
 *	ata_scsi_offline_dev - offline attached SCSI device
 *	@dev: ATA device to offline attached SCSI device for
 *
 *	This function is called from ata_eh_hotplug() and responsible
 *	for taking the SCSI device attached to @dev offline.  This
 *	function is called with host lock which protects dev-&gt;sdev
 *	against clearing.
 *
 *	LOCKING:
 *	spin_lock_irqsave(host lock)
 *
 *	RETURNS:
 *	1 if attached SCSI device exists, 0 otherwise.
 */</doc>
