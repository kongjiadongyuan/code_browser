<dec f='linux-5.3.1/include/acpi/pcc.h' l='14' type='struct mbox_chan * pcc_mbox_request_channel(struct mbox_client * cl, int subspace_id)'/>
<use f='linux-5.3.1/drivers/acpi/cppc_acpi.c' l='546' u='c' c='register_pcc_channel'/>
<def f='linux-5.3.1/drivers/mailbox/pcc.c' l='230' ll='277' type='struct mbox_chan * pcc_mbox_request_channel(struct mbox_client * cl, int subspace_id)'/>
<dec f='linux-5.3.1/drivers/mailbox/pcc.c' l='278' type='struct mbox_chan * pcc_mbox_request_channel(struct mbox_client * , int )'/>
<use f='linux-5.3.1/drivers/mailbox/pcc.c' l='278' c='pcc_mbox_request_channel'/>
<use f='linux-5.3.1/drivers/mailbox/pcc.c' l='278' u='a'/>
<doc f='linux-5.3.1/drivers/mailbox/pcc.c' l='217'>/**
 * pcc_mbox_request_channel - PCC clients call this function to
 *		request a pointer to their PCC subspace, from which they
 *		can get the details of communicating with the remote.
 * @cl: Pointer to Mailbox client, so we know where to bind the
 *		Channel.
 * @subspace_id: The PCC Subspace index as parsed in the PCC client
 *		ACPI package. This is used to lookup the array of PCC
 *		subspaces as parsed by the PCC Mailbox controller.
 *
 * Return: Pointer to the Mailbox Channel if successful or
 *		ERR_PTR.
 */</doc>
