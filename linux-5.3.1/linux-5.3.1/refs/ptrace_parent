<def f='linux-5.3.1/include/linux/ptrace.h' l='124' ll='129' type='struct task_struct * ptrace_parent(struct task_struct * task)'/>
<doc f='linux-5.3.1/include/linux/ptrace.h' l='113'>/**
 * ptrace_parent - return the task that is tracing the given task
 * @task: task to consider
 *
 * Returns %NULL if no one is tracing @task, or the &amp;struct task_struct
 * pointer to its tracer.
 *
 * Must called under rcu_read_lock().  The pointer returned might be kept
 * live only by RCU.  During exec, this may be called with task_lock() held
 * on @task, still held from when check_unsafe_exec() was called.
 */</doc>
<use f='linux-5.3.1/fs/proc/array.c' l='167' u='c' c='task_state'/>
<use f='linux-5.3.1/security/selinux/hooks.c' l='2265' u='c' c='ptrace_parent_sid'/>
