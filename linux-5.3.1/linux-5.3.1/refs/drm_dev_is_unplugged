<def f='linux-5.3.1/include/drm/drm_drv.h' l='758' ll='768' type='bool drm_dev_is_unplugged(struct drm_device * dev)'/>
<doc f='linux-5.3.1/include/drm/drm_drv.h' l='745'>/**
 * drm_dev_is_unplugged - is a DRM device unplugged
 * @dev: DRM device
 *
 * This function can be called to check whether a hotpluggable is unplugged.
 * Unplugging itself is singalled through drm_dev_unplug(). If a device is
 * unplugged, these two functions guarantee that any store before calling
 * drm_dev_unplug() is visible to callers of this function after it completes
 *
 * WARNING: This function fundamentally races against drm_dev_unplug(). It is
 * recommended that drivers instead use the underlying drm_dev_enter() and
 * drm_dev_exit() function pairs.
 */</doc>
<use f='linux-5.3.1/drivers/gpu/drm/drm_drv.c' l='254' u='c' c='drm_minor_acquire'/>
<use f='linux-5.3.1/drivers/gpu/drm/drm_gem.c' l='1151' u='c' c='drm_gem_mmap'/>
<use f='linux-5.3.1/drivers/gpu/drm/drm_ioctl.c' l='772' u='c' c='drm_ioctl_kernel'/>
<use f='linux-5.3.1/drivers/gpu/drm/drm_ioctl.c' l='821' u='c' c='drm_ioctl'/>
