<dec f='linux-5.3.1/drivers/gpu/drm/i915/intel_wopcm.h' l='43' type='int intel_wopcm_init(struct intel_wopcm * wopcm)'/>
<use f='linux-5.3.1/drivers/gpu/drm/i915/i915_gem.c' l='1520' u='c' c='i915_gem_init'/>
<def f='linux-5.3.1/drivers/gpu/drm/i915/intel_wopcm.c' l='164' ll='226' type='int intel_wopcm_init(struct intel_wopcm * wopcm)'/>
<doc f='linux-5.3.1/drivers/gpu/drm/i915/intel_wopcm.c' l='152'>/**
 * intel_wopcm_init() - Initialize the WOPCM structure.
 * @wopcm: pointer to intel_wopcm.
 *
 * This function will partition WOPCM space based on GuC and HuC firmware sizes
 * and will allocate max remaining for use by GuC. This function will also
 * enforce platform dependent hardware restrictions on GuC WOPCM offset and
 * size. It will fail the WOPCM init if any of these checks were failed, so that
 * the following GuC firmware uploading would be aborted.
 *
 * Return: 0 on success, non-zero error code on failure.
 */</doc>
