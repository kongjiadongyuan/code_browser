<def f='linux-5.3.1/include/linux/device.h' l='824' ll='831'/>
<use f='linux-5.3.1/include/linux/device.h' l='868'/>
<doc f='linux-5.3.1/include/linux/device.h' l='815'>/**
 * enum device_link_state - Device link states.
 * @DL_STATE_NONE: The presence of the drivers is not being tracked.
 * @DL_STATE_DORMANT: None of the supplier/consumer drivers is present.
 * @DL_STATE_AVAILABLE: The supplier driver is present, but the consumer is not.
 * @DL_STATE_CONSUMER_PROBE: The consumer is probing (supplier driver present).
 * @DL_STATE_ACTIVE: Both the supplier and consumer drivers are present.
 * @DL_STATE_SUPPLIER_UNBIND: The supplier driver is unbinding.
 */</doc>
<use f='linux-5.3.1/drivers/base/core.c' l='777' c='device_links_unbind_consumers'/>
