<dec f='linux-5.3.1/include/linux/clk.h' l='659' type='bool clk_has_parent(struct clk * clk, struct clk * parent)'/>
<doc f='linux-5.3.1/include/linux/clk.h' l='649'>/**
 * clk_has_parent - check if a clock is a possible parent for another
 * @clk: clock source
 * @parent: parent clock source
 *
 * This function can be used in drivers that need to check that a clock can be
 * the parent of another without actually changing the parent.
 *
 * Returns true if @parent is a possible parent for @clk, false otherwise.
 */</doc>
<def f='linux-5.3.1/drivers/clk/clk.c' l='2424' ll='2445' type='bool clk_has_parent(struct clk * clk, struct clk * parent)'/>
<dec f='linux-5.3.1/drivers/clk/clk.c' l='2446' type='bool clk_has_parent(struct clk * , struct clk * )'/>
<use f='linux-5.3.1/drivers/clk/clk.c' l='2446' c='clk_has_parent'/>
<use f='linux-5.3.1/drivers/clk/clk.c' l='2446' u='a'/>
<doc f='linux-5.3.1/drivers/clk/clk.c' l='2414'>/**
 * clk_has_parent - check if a clock is a possible parent for another
 * @clk: clock source
 * @parent: parent clock source
 *
 * This function can be used in drivers that need to check that a clock can be
 * the parent of another without actually changing the parent.
 *
 * Returns true if @parent is a possible parent for @clk, false otherwise.
 */</doc>
